#include <stdio.h>
#include <conio.h>
#include <stdlib.h> // ???????????? atoi

main()
{
    char str1[20];
    int x;
    printf("Enter Your Number: ");
    gets(str1); // ????????????????? (String)
    x = atoi(str1); // ??????????????????????????????????
    printf("%d\n", x);
    getch();
}
